apiVersion: v1
kind: Service
metadata:
  name: {{ include "project_service_deployment.fullname" . }}
  labels:
    {{- include "project_service_deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "project_service_deployment.selectorLabels" . | nindent 4 }}
